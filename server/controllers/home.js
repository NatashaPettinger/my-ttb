/* Fix this to render the homepage w/ menu etc. 
Probably can just use EJS for this since nothing will change. 
But, would be easier to make menu/nav/sidebar module in react */


module.exports = {
    /* getIndex: (req,res)=>{
        res.render('index.ejs')
    } */
}